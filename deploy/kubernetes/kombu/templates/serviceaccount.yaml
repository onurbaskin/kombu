{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kombu.serviceAccountName" . }}
  labels:
    {{- include "kombu.labels" . | nindent 4 }}
{{- end }}
